# Case 4.25 - KNOWN GAP (xfail): `disas <symbol>` and `print <global>`.
# Target: test_basic.exe
# GDB resolves PDB symbols for both; aidbg's parse_addr() does not, so
# `disas func2, func2+0x10` -> "bad address" and `print global_var` ->
# "cannot parse expression". Kept as xfail so the gap stays visible while
# the suite stays green.
file test_basic.exe
start
break func2
continue
disas func2, func2+0x10
print global_var
quit
